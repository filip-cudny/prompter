<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Allow JIT compilation (required for Python) -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>

    <!-- Allow unsigned executable memory (required for PyInstaller) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Disable library validation (required for bundled libraries) -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!-- Microphone access (required for speech-to-text) -->
    <key>com.apple.security.device.audio-input</key>
    <true/>

    <!-- Network client access (required for OpenAI API) -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- Input monitoring (required for global hotkeys via pynput) -->
    <key>com.apple.security.input-monitoring</key>
    <true/>
</dict>
</plist>
